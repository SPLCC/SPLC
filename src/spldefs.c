#include "spldefs.h"

const char* get_spltype_string(spltype_t type) {
    switch (type)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
}